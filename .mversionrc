{
  "commitMessage": "Release of version v%s.",
  "scripts": {
    "postcommit": "echo 'Version updated to v%s. Commit ready, tag added - run `git push && git push --tags` to finish the release by pushing it into 'develop' or 'master' branch.'"
  },
  "updatedFiles": [
    "package.json",
    "./build/release-templates/package.json"
  ]
}
